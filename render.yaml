services:
  - type: web
    name: bioid-udochain
    env: node
    plan: free
    autoDeploy: true

    buildCommand: |
      echo "🚀 Building BioID (WebAuthn Biometric Server)..."
      npm install

    startCommand: node server.js

    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 3000
